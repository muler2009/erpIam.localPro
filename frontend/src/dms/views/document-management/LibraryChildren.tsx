import React from 'react'
import { LibraryChildrenInterface, LibraryInterface } from '../../models/library-models'
import { FlexBox } from '../../../components/common/StyledComponent'

interface LibraryInterace2 {
    library: LibraryInterface[]
}

const LibraryChildren = () => {
  return (
    <FlexBox>
      lalalalal
    </FlexBox>
  )
}

export default LibraryChildren